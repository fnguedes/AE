import { TouchableOpacity, Text } from 'react-native'
import React from 'react'
import style from './Style/styleOptionsFiltro'

export default function OptionsFiltro() {

  return (
    <TouchableOpacity style={style.container} onPress={()=>{}}>
    </TouchableOpacity>
  )
}